# Fishery summary
#
# Return the most frequently caught fish
# Sum total revenue for each location
# Sum total fishery revenue
# Graph revenue by location with total revenue label if user requests

fishery_summary = function (price, landing) {
  most_frequent = max (landing)
  return(most_frequent)
}